import { makeStyles } from '@material-ui/core';


const useHeaderSectionStyles = makeStyles({
    root: {
        flexGrow: 1,
        minHeight: 50,
    },
    appBar: {
        padding: 10,
    }
});

export default useHeaderSectionStyles;